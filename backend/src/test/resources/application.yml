# Copyright 2026 Shanghai Rujing Zhihua Information Technology Co., Ltd. · https://www.zhuatech.cn/
spring:
  datasource:
    url: jdbc:h2:mem:zhuatech;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: create-drop
app:
  jwt:
    secret: test-zhuatech-secret-key-at-least-32-bytes-long
  cors:
    # 模拟旧版 .env：后端仍须自动兼容 127.0.0.1:8088。
    allowed-origins: http://localhost:8088
